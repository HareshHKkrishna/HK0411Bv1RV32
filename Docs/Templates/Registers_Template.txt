Purpose : Present in the ID stage, Used to read and write data's in it so the ALU could fetch and execute the values.

Inputs :

    1. [4:0]rs1, //read source register 1,
    2. [4:0]rs2. //read source register 2,
    3. [31:0]wr_data, // write destination register. 
    4. [4:0]wr_addr, // write data address.
    5. wr_en  // reg_write_enable.
    
    outputS:

    1. output logic [31:0]rs1_data,
    2. output logic [31:0]rs2_data,

Sequential or Combinational 

Sequential

Needs clk ?
yes
Needs rst ? 
yes
Needs stall ?  
no
Needs flush ?  
no
